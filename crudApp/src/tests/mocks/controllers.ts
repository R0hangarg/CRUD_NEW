// import { NextFunction, Request, Response } from "express";

// export const isAuthenicated = (req:Request, res:Response, next:NextFunction) => {
//     next(); // Call next() to skip authentication
//   };